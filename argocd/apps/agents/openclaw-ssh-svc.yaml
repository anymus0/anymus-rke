apiVersion: v1
kind: Service
metadata:
  name: openclaw-ssh
  namespace: agents
  labels:
    app: openclaw
spec:
  type: ClusterIP
  selector:
    vm.kubevirt.io/name: openclaw
  ports:
    - name: ssh
      port: 22
      targetPort: 22
      protocol: TCP