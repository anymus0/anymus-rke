apiVersion: v1
kind: Service
metadata:
  name: hermes-ssh
  namespace: agents
  labels:
    app: hermes
spec:
  type: ClusterIP
  selector:
    vm.kubevirt.io/name: hermes
  ports:
    - name: ssh
      port: 22
      targetPort: 22
      protocol: TCP