apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: immich
  namespace: argocd
spec:
  project: default
  destination:
    server: "https://kubernetes.default.svc"
    namespace: cloud
  source:
    repoURL: https://github.com/immich-app/immich-charts.git
    targetRevision: 4cde6937cd2fc12458bb1d8f014888f20b3fc193
    path: charts/immich
    helm:
      releaseName: immich
      valuesObject:
        controllers:
          main:
            containers:
              main:
                # image:
                #   tag: commit-2b27e6183d55cc2aace1e305dd698e3b36bb5688
                env:
                  DB_HOSTNAME:
                    valueFrom:
                      secretKeyRef:
                        name: immich-postgresql-creds
                        key: host
                  DB_USERNAME:
                    valueFrom:
                      secretKeyRef:
                        name: immich-postgresql-creds
                        key: username
                  DB_PASSWORD:
                    valueFrom:
                      secretKeyRef:
                        name: immich-postgresql-creds
                        key: password
                  DB_DATABASE_NAME:
                    valueFrom:
                      secretKeyRef:
                        name: immich-postgresql-creds
                        key: dbname
        immich:
          metrics:
            enabled: true
          persistence:
            library:
              existingClaim: immich-default-vol
        server:
          ingress:
            main:
              enabled: false
        machine-learning:
          persistence:
            cache:
              enabled: true
              type: persistentVolumeClaim
              size: 20Gi
              accessMode: ReadWriteMany
              storageClass: longhorn
        valkey:
          enabled: true
          persistence:
            data:
              enabled: true
              type: persistentVolumeClaim
              size: 1Gi
              accessMode: ReadWriteOnce
              storageClass: longhorn
